# Audit Controls

Audit review checks JSON/JSONL evidence for required event fields, duplicate event IDs, ISO-like timestamps, and secret-like values. Append-only consistency is reviewed where feasible from local artifacts only.
